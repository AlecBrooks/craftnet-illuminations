# Lantern

The browser. Runs as a CraftNet Host.

Not started. Expected shape: an address bar, a search bar, a full-screen dashboard-style UI, `cnet.request()` to fetch a page's LCM content, a renderer, and hyperlink navigation that turns into further requests.
