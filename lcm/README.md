# LCM (Light Craft Markup)

The content format Lamp serves and Lantern renders.

Not started. Needs: a spec, a parser, and a renderer (used by Lantern). Worth deciding early whether a single `.lcm` file is expected to fit inside CraftNet's per-message size limit, or whether pagination/chunking is part of the format from the start.
